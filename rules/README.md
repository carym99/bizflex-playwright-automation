# Playwright QA Rules

This folder contains the shared test standards for this Playwright project.

- Primary standard doc: `rules/testing.mdc`
- Keep secrets out of docs and specs; use env vars from `.env.local` / CI secrets.
- Mirror Cypress behavior where parity is required.

