/**
 * @deprecated Import from `./selectAccount` — kept for backward-compatible imports.
 */
export {
  resolveSelectAccountToDashboardIfNeeded,
  selectAccountOnPicker,
  assertActiveAccountContext,
  type SelectAccountResult,
} from './selectAccount';
